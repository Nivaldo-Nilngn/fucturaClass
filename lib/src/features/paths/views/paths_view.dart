import 'package:flutter/material.dart';

class PathsView extends StatelessWidget {
  const PathsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Trilhas')),
    );
  }
}
